interface:
  display_name: "Liepin Jobs"
  short_description: "Search and manage Liepin jobs and resume tasks."
  default_prompt: "Use $liepin-jobs to search roles on Liepin, review my resume, and prepare safe job applications."

policy:
  allow_implicit_invocation: true
